# V1 版本 UI/UX 和前端设计提示词

## 设计目标

基于知识星球训练营自动退款系统的业务需求，设计简洁、高效、用户友好的前端界面，满足会员端和管理端的不同使用场景。

**核心设计原则**：
- **简洁优先**：减少认知负担，核心功能一目了然
- **移动优先**：H5 端优先考虑移动端体验
- **高效操作**：减少操作步骤，提升管理效率
- **数据可视化**：关键数据用图表直观展示
- **一致性**：统一的设计语言和交互规范

---

## 技术栈选型

### H5 会员端
- **框架**：Vue 3 + Vite
- **UI 组件库**：Vant 4.x (专为移动端优化)
- **路由**：Vue Router 4
- **状态管理**：Pinia
- **网络请求**：Axios
- **样式方案**：
  - CSS Modules (组件级样式隔离)
  - Less/Sass 预处理器
  - PostCSS + Autoprefixer (自动添加浏览器前缀)
- **工具库**：
  - Day.js (日期处理)
  - Lodash-es (工具函数)
  - html2canvas (生成分享图片)
- **微信集成**：
  - JSSDK (微信分享、支付)
  - 企业微信支付 SDK

### Web 管理后台
- **框架**：Vue 3 + Vite
- **UI 组件库**：Element Plus (企业级中后台 UI)
- **路由**：Vue Router 4
- **状态管理**：Pinia
- **网络请求**：Axios
- **图表库**：ECharts 5.x
- **表格增强**：Element Plus Table + 虚拟滚动
- **富文本编辑**：Quill / TinyMCE (编辑 SOP 教程)
- **工具库**：
  - Day.js (日期处理)
  - Lodash-es (工具函数)
  - XLSX (Excel 导入导出)

### 共享基础设施
- **代码规范**：ESLint + Prettier
- **Git Hooks**：Husky + lint-staged
- **版本控制**：Conventional Commits
- **构建工具**：Vite 5.x
- **包管理器**：pnpm (节省磁盘空间，提升安装速度)
- **环境变量**：.env 文件管理
- **Mock 数据**：Vite Plugin Mock (开发阶段)

---

## H5 会员端 UI 设计

### 整体布局

**设计风格**：
- 清新简洁，符合知识付费产品调性
- 主色调：蓝色系 (#1989FA - Vant 主题色)
- 辅助色：成功绿 (#07C160)、警告橙 (#FF976A)、错误红 (#EE0A24)
- 背景色：浅灰 (#F7F8FA)，卡片白色 (#FFFFFF)

**导航结构**：
- 底部 Tab 导航 (3 个标签)
  - 训练营 (主页)
  - 我的项目 (个人中心)
  - SOP 教程 (帮助文档)

**适配方案**：
- 使用 `viewport` 单位 (vw/vh) 实现响应式
- 字体大小使用 `rem`，基于设计稿 375px 宽度
- 关键触控区域最小 44px × 44px

---

### 页面详细设计

#### 1. 训练营列表页 (首页)

**路由**：`/camps`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  知识星球训练营               │
│  [筛选按钮] [搜索框]         │
├─────────────────────────────┤
│  [状态筛选 Tab]              │
│  进行中 | 即将开始 | 已结束  │
├─────────────────────────────┤
│  [训练营卡片 1]              │
│  ┌───────────────────────┐  │
│  │ [封面图]    [状态标签] │  │
│  │ 训练营标题             │  │
│  │ 时间: 2025-01-01 开营 │  │
│  │ 押金: ¥99  已报名 50人│  │
│  │ [立即报名] 按钮        │  │
│  └───────────────────────┘  │
│                             │
│  [训练营卡片 2]              │
│  ...                        │
│                             │
│  [加载更多]                 │
└─────────────────────────────┘
```

**核心功能**：
- 下拉刷新，上拉加载更多
- 状态筛选：进行中、即将开始、已结束
- 搜索：支持按训练营名称搜索
- 排序：默认按开营时间倒序

**交互细节**：
- 卡片点击进入训练营详情页
- 状态标签颜色区分：
  - 进行中：绿色
  - 即将开始：蓝色
  - 已结束：灰色
- "立即报名" 按钮：
  - 未报名：蓝色按钮
  - 已报名：灰色 "已报名" 文字
  - 已结束：隐藏按钮

**数据字段**：
```typescript
interface Camp {
  id: string;              // 训练营 ID
  title: string;           // 标题
  coverImage: string;      // 封面图
  startDate: string;       // 开始时间
  endDate: string;         // 结束时间
  deposit: number;         // 押金金额
  status: 'ongoing' | 'upcoming' | 'ended'; // 状态
  enrollCount: number;     // 已报名人数
  maxEnroll: number;       // 最大报名人数
  isEnrolled: boolean;     // 当前用户是否已报名
}
```

---

#### 2. 训练营详情页

**路由**：`/camps/:id`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部导航栏] [分享按钮]     │
├─────────────────────────────┤
│  [封面图轮播]                │
├─────────────────────────────┤
│  训练营标题                  │
│  时间: 2025-01-01 - 01-10   │
│  押金: ¥99  已报名 50/100人 │
├─────────────────────────────┤
│  [Tab 切换]                 │
│  详情 | 规则 | 打卡榜       │
├─────────────────────────────┤
│  [详情内容区域]              │
│  ● 训练营介绍 (富文本)       │
│  ● 教练介绍                  │
│  ● 志愿者团队                │
│  ● 项目收获                  │
├─────────────────────────────┤
│  [底部操作栏]                │
│  [咨询客服] [立即报名 ¥99]  │
└─────────────────────────────┘
```

**核心功能**：
- 训练营详细信息展示
- Tab 切换查看不同维度信息
- 报名操作入口
- 分享功能（生成海报）

**Tab 内容设计**：

**详情 Tab**：
- 训练营介绍（富文本，支持图片、视频）
- 教练信息（头像、昵称、简介）
- 志愿者团队（多人列表）
- 项目收获（列表展示）

**规则 Tab**：
- 押金金额及退款规则
- 打卡要求（总天数、需打卡天数）
- 打卡时间段（每天几点到几点）
- 违规处理规则
- 退款时间说明

**打卡榜 Tab**：
- 实时打卡排行榜
- 显示：排名、头像、昵称、打卡天数
- 支持筛选：全部/已完成/未完成
- 当前用户高亮显示

**交互细节**：
- "立即报名" 按钮：
  - 未登录：提示登录（弹窗）
  - 已报名：显示 "已报名，查看我的进度"
  - 点击跳转支付页面
- 分享功能：
  - 生成训练营海报（包含二维码）
  - 保存到相册或分享到微信

---

#### 3. 支付页面

**路由**：`/payment/:campId`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  支付押金                     │
├─────────────────────────────┤
│  [订单信息卡片]              │
│  ┌───────────────────────┐  │
│  │ 训练营名称             │  │
│  │ 时间: 2025-01-01      │  │
│  │ 押金: ¥99             │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  [支付方式选择]              │
│  ○ 微信支付 (默认选中)       │
│  ○ 企业微信支付              │
├─────────────────────────────┤
│  [用户信息填写]              │
│  ┌───────────────────────┐  │
│  │ 星球昵称 (自动填充)    │  │
│  │ [输入框]               │  │
│  │ 提示: 用于核对退款身份 │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  [押金说明]                 │
│  ● 完成打卡后自动退款        │
│  ● 预计退款时间: 项目结束后 │
│    3 个工作日               │
│  ● 退款方式: 原路退回        │
├─────────────────────────────┤
│  [底部操作栏]                │
│  [取消]  [确认支付 ¥99]     │
└─────────────────────────────┘
```

**核心功能**：
- 订单信息确认
- 支付方式选择
- 用户信息填写（星球昵称）
- 调起企业微信支付

**交互细节**：
- 星球昵称自动填充（从用户资料获取）
- 支付按钮防重复点击（Loading 状态）
- 支付成功后：
  - 显示成功提示
  - 弹出群二维码（可保存）
  - 3 秒后自动跳转到 "我的项目" 页
- 支付失败：
  - 显示失败原因
  - 提供重新支付按钮

**支付流程**：
1. 用户点击 "确认支付"
2. 调用后端创建支付订单接口
3. 后端返回支付参数（企微支付）
4. 前端调起企业微信支付 SDK
5. 支付完成后回调前端
6. 前端通知后端支付结果
7. 展示群二维码

---

#### 4. 我的项目页

**路由**：`/my-projects`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  我的项目                     │
├─────────────────────────────┤
│  [状态筛选 Tab]              │
│  全部 | 进行中 | 已完成      │
├─────────────────────────────┤
│  [项目卡片 1]                │
│  ┌───────────────────────┐  │
│  │ [封面图]  [进行中]     │  │
│  │ 训练营标题             │  │
│  │ 打卡进度: 5/7 天       │  │
│  │ 剩余时间: 2 天         │  │
│  │ ────────────── 71%    │  │
│  │ [查看详情] [去打卡]   │  │
│  └───────────────────────┘  │
│                             │
│  [项目卡片 2 - 已完成]      │
│  ┌───────────────────────┐  │
│  │ [封面图]  [已完成✓]    │  │
│  │ 训练营标题             │  │
│  │ 完成打卡: 8/7 天       │  │
│  │ 退款状态: 已退款 ¥99  │  │
│  │ [查看详情] [查看证书] │  │
│  └───────────────────────┘  │
│                             │
│  [空状态提示]                │
│  暂无项目                    │
│  [去报名训练营]              │
└─────────────────────────────┘
```

**核心功能**：
- 查看已报名的训练营列表
- 按状态筛选（全部、进行中、已完成）
- 实时显示打卡进度
- 查看退款状态

**卡片状态设计**：

**进行中**：
- 显示打卡进度条
- 显示剩余天数
- "去打卡" 按钮（跳转知识星球）
- 打卡进度用颜色区分：
  - 进度 >= 80%：绿色
  - 进度 50%-80%：橙色
  - 进度 < 50%：红色

**已完成**：
- 显示完成状态（成功/失败）
- 显示退款状态：
  - 待退款：橙色
  - 已退款：绿色
  - 不符合退款条件：灰色
- "查看证书" 按钮（完成打卡的用户）

**未开始**：
- 显示开营倒计时
- "查看详情" 按钮

---

#### 5. 项目详情页 (我的)

**路由**：`/my-projects/:id`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  项目详情                     │
├─────────────────────────────┤
│  [项目基础信息]              │
│  训练营标题                  │
│  时间: 2025-01-01 - 01-10   │
│  我的打卡: 5/7 天 ✓ 达标     │
├─────────────────────────────┤
│  [打卡日历]                  │
│  一 二 三 四 五 六 日        │
│  ✓  ✓  ✗  ✓  ✓  ✓  ✗       │
│  1  2  3  4  5  6  7        │
│                             │
│  ✓ 已打卡  ✗ 未打卡  ○ 未开始│
├─────────────────────────────┤
│  [退款信息]                  │
│  押金: ¥99                  │
│  退款状态: 已退款 ✓          │
│  退款时间: 2025-01-15       │
│  退款方式: 企业微信原路退回  │
├─────────────────────────────┤
│  [我的打卡记录]              │
│  [列表展示每天的打卡]        │
│  2025-01-05  已打卡 ✓       │
│  打卡时间: 18:30            │
│  [查看详情]                  │
│                             │
│  2025-01-04  已打卡 ✓       │
│  ...                        │
├─────────────────────────────┤
│  [底部操作栏]                │
│  [去打卡] [查看排行榜]      │
└─────────────────────────────┘
```

**核心功能**：
- 打卡日历可视化
- 打卡记录详情
- 退款状态追踪
- 快捷操作入口

**交互细节**：
- 日历中点击某一天：
  - 已打卡：显示打卡详情（时间、内容）
  - 未打卡：提示原因（未到时间/已过期/忘记打卡）
- "去打卡" 按钮：
  - 如果是今天：跳转知识星球打卡页
  - 如果已过期：显示 "项目已结束"
- 退款状态实时更新：
  - 待审核：橙色图标
  - 退款中：蓝色进度条
  - 已退款：绿色对勾

---

#### 6. SOP 教程页

**路由**：`/tutorial`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  操作指南                     │
├─────────────────────────────┤
│  [搜索框]                    │
│  搜索教程内容                │
├─────────────────────────────┤
│  [分类导航]                  │
│  新手入门 | 支付押金 |       │
│  打卡指南 | 退款说明         │
├─────────────────────────────┤
│  [折叠面板 1] ▼              │
│  如何支付押金？              │
│  ┌───────────────────────┐  │
│  │ 1. 进入训练营详情      │  │
│  │ 2. 点击"立即报名"      │  │
│  │ 3. 填写星球昵称        │  │
│  │ 4. 选择支付方式        │  │
│  │ 5. 完成支付            │  │
│  │ [查看视频教程]         │  │
│  └───────────────────────┘  │
│                             │
│  [折叠面板 2] ▶              │
│  如何进群学习？              │
│                             │
│  [折叠面板 3] ▶              │
│  如何完成打卡？              │
│                             │
│  [折叠面板 4] ▶              │
│  如何查询退款？              │
├─────────────────────────────┤
│  [常见问题]                  │
│  Q: 忘记打卡怎么办？         │
│  A: 联系教练说明情况...      │
│                             │
│  Q: 退款多久到账？           │
│  A: 3 个工作日内...          │
└─────────────────────────────┘
```

**核心功能**：
- 分类浏览教程
- 搜索功能
- 折叠面板展示内容
- 支持富文本、图片、视频

**交互细节**：
- 点击分类快速定位
- 搜索高亮匹配内容
- 折叠面板支持展开/收起
- 支持点击图片放大查看
- 视频支持全屏播放

---

#### 7. 个人中心页

**路由**：`/profile`

**页面布局**：
```
┌─────────────────────────────┐
│  [顶部个人信息卡片]          │
│  ┌───────────────────────┐  │
│  │ [头像]  星球昵称       │  │
│  │         ID: 12345678  │  │
│  │ [编辑资料]             │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  [数据统计]                  │
│  ┌──────┬──────┬──────┐   │
│  │ 参加 │ 完成 │ 退款 │   │
│  │  3   │  2   │ ¥198 │   │
│  │ 训练营│ 项目 │ 总额 │   │
│  └──────┴──────┴──────┘   │
├─────────────────────────────┤
│  [功能菜单]                  │
│  🏆 我的成就                 │
│  📜 我的证书                 │
│  💰 退款记录                 │
│  📱 绑定手机号               │
│  ⚙️  设置                    │
│  📞 联系客服                 │
│  ℹ️  关于我们                 │
└─────────────────────────────┘
```

**核心功能**：
- 个人信息展示和编辑
- 数据统计一览
- 功能菜单入口

---

### H5 端通用组件设计

#### 1. 训练营卡片组件 (CampCard)

**Props**：
```typescript
interface CampCardProps {
  camp: Camp;                    // 训练营数据
  showEnrollButton?: boolean;    // 是否显示报名按钮
  showProgress?: boolean;        // 是否显示进度条（我的项目用）
}
```

**样式变量**：
```css
--card-radius: 12px;
--card-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
--card-padding: 16px;
```

#### 2. 状态标签组件 (StatusTag)

**类型**：
- `ongoing`: 进行中（绿色）
- `upcoming`: 即将开始（蓝色）
- `ended`: 已结束（灰色）
- `success`: 成功（绿色）
- `failed`: 失败（红色）

#### 3. 打卡日历组件 (CheckInCalendar)

**Props**：
```typescript
interface CheckInCalendarProps {
  startDate: string;       // 开始日期
  endDate: string;         // 结束日期
  checkInRecords: Date[];  // 打卡记录
}
```

#### 4. 空状态组件 (EmptyState)

**Props**：
```typescript
interface EmptyStateProps {
  icon?: string;           // 图标
  title: string;           // 标题
  description?: string;    // 描述
  actionText?: string;     // 操作按钮文字
  onAction?: () => void;   // 操作回调
}
```

---

## Web 管理后台 UI 设计

### 整体布局

**设计风格**：
- 专业、高效的企业级后台风格
- 主色调：蓝色系 (#409EFF - Element Plus 主题色)
- 采用侧边栏 + 顶栏布局
- 响应式设计（支持桌面端和平板）

**布局结构**：
```
┌──────────┬─────────────────────────────────┐
│          │  [顶部导航栏]                     │
│          │  [面包屑] [用户信息] [退出]       │
│  [侧边栏] ├─────────────────────────────────┤
│          │                                  │
│  训练营   │                                  │
│  会员     │        [主内容区域]              │
│  退款     │                                  │
│  数据     │                                  │
│  设置     │                                  │
│          │                                  │
│          │                                  │
└──────────┴─────────────────────────────────┘
```

**侧边栏菜单**：
- 🏠 工作台 (Dashboard)
- 🏕️ 训练营管理
  - 训练营列表
  - 新建训练营
  - 批量操作
- 👥 会员管理
  - 会员列表
  - 会员匹配审核
  - 黑名单管理
- 💰 退款管理
  - 待处理退款
  - 退款记录
  - 异常处理
- 📊 数据统计
  - 运营概览
  - 财务报表
  - 打卡分析
- ⚙️ 系统设置
  - 基础配置
  - SOP 教程管理
  - 权限管理
  - 操作日志

---

### 页面详细设计

#### 1. 工作台 (Dashboard)

**路由**：`/dashboard`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [数据概览卡片组]                             │
│  ┌──────────┬──────────┬──────────┬─────┐  │
│  │ 总训练营 │ 进行中   │ 总会员   │总押金│  │
│  │   156   │   23    │  3,542  │125K │  │
│  │  +12↑   │         │  +89↑   │+8K↑ │  │
│  └──────────┴──────────┴──────────┴─────┘  │
├─────────────────────────────────────────────┤
│  [图表区域]                                  │
│  ┌─────────────────┬──────────────────────┐│
│  │ 训练营趋势图     │  打卡完成率分布图     ││
│  │ (折线图)        │  (饼图)              ││
│  │                │                      ││
│  └─────────────────┴──────────────────────┘│
│  ┌───────────────────────────────────────┐ │
│  │ 近 7 天押金收入趋势 (柱状图)           │ │
│  │                                       │ │
│  └───────────────────────────────────────┘ │
├─────────────────────────────────────────────┤
│  [待办事项列表]                              │
│  ● 5 个训练营即将结束，需要处理退款           │
│  ● 12 个会员匹配待审核                       │
│  ● 3 个异常退款需要处理                      │
│  [查看全部]                                  │
├─────────────────────────────────────────────┤
│  [快捷操作]                                  │
│  [新建训练营] [导入会员] [批量退款]          │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 关键指标实时展示
- 数据趋势可视化
- 待办事项提醒
- 快捷操作入口

**图表设计**：
- 训练营趋势图：近 30 天的新建/进行中/已结束数量
- 打卡完成率分布：完成率区间统计（0-60%、60-80%、80-100%）
- 押金收入趋势：近 7 天每天的押金收入金额

---

#### 2. 训练营列表页

**路由**：`/camps`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] 训练营管理                        │
│  [操作按钮组] [新建训练营] [批量导入]         │
├─────────────────────────────────────────────┤
│  [搜索筛选区域]                               │
│  关键词: [输入框]  状态: [下拉选择]           │
│  时间: [日期范围]  [搜索] [重置]              │
├─────────────────────────────────────────────┤
│  [数据表格]                                  │
│  ┌───┬────────┬──────┬──────┬──────┬─────┐│
│  │☑ │ 训练营  │ 状态 │ 时间 │ 报名 │操作 ││
│  ├───┼────────┼──────┼──────┼──────┼─────┤│
│  │☑ │7天早起营│进行中│01-01 │ 50  │编辑 ││
│  │  │押金¥99 │      │01-07 │ /100│查看 ││
│  │  │        │      │      │     │删除 ││
│  ├───┼────────┼──────┼──────┼──────┼─────┤│
│  │☑ │阅读打卡│已结束│12-20 │ 80  │查看 ││
│  │  │押金¥49 │      │12-31 │ /100│退款 ││
│  │  │        │      │      │     │详情 ││
│  └───┴────────┴──────┴──────┴──────┴─────┘│
│                                             │
│  [分页] 共 156 条  [上一页] 1 2 3 [下一页]  │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 训练营列表展示
- 多条件搜索筛选
- 批量操作（批量删除、批量导出）
- 快速新建训练营

**表格字段**：
- 训练营名称 (可点击查看详情)
- 状态（进行中、即将开始、已结束）
- 时间范围
- 报名人数 / 最大人数
- 押金金额
- 操作按钮（编辑、查看、删除、退款处理）

**交互细节**：
- 表头可排序（按时间、报名人数）
- 行选择（多选框）
- 状态用不同颜色的标签区分
- 操作按钮下拉菜单（更多操作）

---

#### 3. 新建/编辑训练营页

**路由**：`/camps/create` 或 `/camps/:id/edit`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] 新建训练营                        │
│  [返回] [保存草稿] [发布]                     │
├─────────────────────────────────────────────┤
│  [表单区域 - 分步骤]                          │
│  ┌─────────────────────────────────────────┐│
│  │ Step 1: 基础信息 ✓                       ││
│  │ Step 2: 规则设置 (当前)                  ││
│  │ Step 3: 人员配置                         ││
│  │ Step 4: 预览发布                         ││
│  └─────────────────────────────────────────┘│
├─────────────────────────────────────────────┤
│  [Step 2: 规则设置]                          │
│  ┌─────────────────────────────────────────┐│
│  │ 押金设置:                                ││
│  │   押金金额: [99] 元                      ││
│  │   最大报名: [100] 人                     ││
│  │                                          ││
│  │ 打卡规则:                                ││
│  │   训练营总天数: [7] 天                   ││
│  │   要求打卡天数: [5] 天                   ││
│  │   打卡时间段: [00:00] - [23:59]         ││
│  │   □ 允许补卡 (最多 [1] 次)              ││
│  │                                          ││
│  │ 退款规则:                                ││
│  │   ○ 自动退款 (打卡达标后)                ││
│  │   ○ 人工审核后退款                       ││
│  │   宽限天数: [1] 天                       ││
│  │   退款时间: 项目结束后 [3] 个工作日      ││
│  └─────────────────────────────────────────┘│
├─────────────────────────────────────────────┤
│  [操作按钮]                                  │
│  [上一步] [下一步] [保存草稿]                │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 分步骤表单填写
- 实时预览效果
- 保存草稿
- 表单验证

**表单分步**：

**Step 1: 基础信息**
- 训练营名称 (必填)
- 副标题
- 封面图上传 (支持裁剪)
- 详细介绍 (富文本编辑器)
- 时间范围 (开始日期、结束日期)
- 知识星球打卡项目 ID (关联)

**Step 2: 规则设置**
- 押金金额
- 最大报名人数
- 打卡总天数 / 要求天数
- 打卡时间段
- 补卡规则
- 退款方式（自动/人工）

**Step 3: 人员配置**
- 项目教练 (多选)
- 志愿者 (多选)
- 客服联系方式

**Step 4: 预览发布**
- H5 端效果预览
- 信息确认
- 发布时间选择（立即发布 / 定时发布）

**交互细节**：
- 步骤间可前后切换
- 表单数据自动保存（localStorage）
- 上传图片支持拖拽
- 富文本编辑器支持插入图片、视频、链接

---

#### 4. 会员管理页

**路由**：`/members`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] 会员管理                          │
│  [操作按钮] [导入会员] [导出Excel]            │
├─────────────────────────────────────────────┤
│  [搜索筛选]                                  │
│  关键词: [输入框]  状态: [下拉]  [搜索]       │
├─────────────────────────────────────────────┤
│  [数据表格]                                  │
│  ┌────┬────────┬────────┬──────┬──────┬───┐│
│  │ ID │ 星球昵称│ 微信昵称│ 参加 │ 完成 │操作││
│  ├────┼────────┼────────┼──────┼──────┼───┤│
│  │8845│球球的副│ 小球球 │  3   │  2   │查看││
│  │    │业探索  │        │      │      │编辑││
│  ├────┼────────┼────────┼──────┼──────┼───┤│
│  │1234│Aaron  │ Aaron  │  5   │  4   │查看││
│  │    │       │        │      │      │编辑││
│  └────┴────────┴────────┴──────┴──────┴───┘│
│                                             │
│  [分页] 共 3,542 条                          │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 会员列表展示
- 会员信息管理
- 导入导出功能
- 会员详情查看

**表格字段**：
- 星球用户 ID
- 星球昵称
- 微信昵称
- 手机号
- 参加训练营数量
- 完成训练营数量
- 总押金金额
- 总退款金额
- 注册时间

---

#### 5. 会员匹配审核页

**路由**：`/members/match`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] 会员匹配审核                      │
│  待审核: 12 条                               │
├─────────────────────────────────────────────┤
│  [匹配记录卡片 1]                             │
│  ┌─────────────────────────────────────────┐│
│  │ 训练营: 7 天早起营                        ││
│  │                                          ││
│  │ 支付记录:                  知识星球用户:  ││
│  │ ┌──────────────┐         ┌────────────┐ ││
│  │ │ 微信昵称: 小球球│    ←?→  │ 星球ID: 8845│ ││
│  │ │ 支付金额: ¥99 │         │ 昵称: 球球的││
│  │ │ 备注: 球球     │         │      副业探索││
│  │ │ 支付时间:     │         │            │ ││
│  │ │  2025-01-01  │         │            │ ││
│  │ └──────────────┘         └────────────┘ ││
│  │                                          ││
│  │ 匹配建议: 昵称相似度 66%  [中等置信度]   ││
│  │                                          ││
│  │ [确认匹配] [手动搜索] [标记异常]         ││
│  └─────────────────────────────────────────┘│
│                                             │
│  [匹配记录卡片 2]                             │
│  ...                                        │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 智能匹配算法结果展示
- 人工审核确认
- 手动搜索匹配
- 异常标记处理

**匹配状态**：
- 高置信度（100%）：星球 ID 完全匹配 → 自动通过
- 中等置信度（66%）：昵称相似 → 需人工确认
- 低置信度（33%）：仅微信昵称相似 → 需人工确认
- 无匹配：标记异常，人工处理

**交互细节**：
- "确认匹配"：直接通过审核
- "手动搜索"：弹出搜索框，输入关键词查找用户
- "标记异常"：标记为问题订单，待人工联系会员

---

#### 6. 退款管理页

**路由**：`/refunds`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] 退款管理                          │
│  [Tab 切换] 待处理(5) | 处理中(12) | 已完成  │
├─────────────────────────────────────────────┤
│  [批量操作] [批量通过] [批量拒绝] [导出]      │
├─────────────────────────────────────────────┤
│  [数据表格 - 待处理]                          │
│  ┌──┬────────┬────┬─────┬──────┬──────┬───┐│
│  │☑│ 训练营  │会员│打卡 │ 押金 │ 状态 │操作││
│  ├──┼────────┼────┼─────┼──────┼──────┼───┤│
│  │☑│早起营  │球球│ 7/7 │ ¥99 │待审核│通过││
│  │ │        │    │ ✓达标│     │      │拒绝││
│  │ │        │    │     │     │      │详情││
│  ├──┼────────┼────┼─────┼──────┼──────┼───┤│
│  │☑│阅读营  │Aaron│5/7 │ ¥49 │待审核│通过││
│  │ │        │    │✗未达标│    │      │拒绝││
│  │ │        │    │     │     │      │详情││
│  └──┴────────┴────┴─────┴──────┴──────┴───┘│
│                                             │
│  [分页]                                      │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 退款申请列表
- 批量审核通过/拒绝
- 退款状态追踪
- 异常退款处理

**状态流转**：
1. 待审核 → 审核通过 → 退款中 → 已退款
2. 待审核 → 审核拒绝 → 已拒绝
3. 退款中 → 退款失败 → 重试/异常处理

**交互细节**：
- "通过" 按钮：弹出确认框，显示退款金额和方式
- "拒绝" 按钮：弹出对话框，填写拒绝原因
- "详情" 按钮：打开侧边抽屉，显示完整信息
- 批量操作：选中多条记录后批量处理

---

#### 7. 退款详情抽屉

**组件类型**：Drawer (侧边抽屉)

**内容布局**：
```
┌─────────────────────────────┐
│  [关闭按钮 ×]                 │
│  退款详情                     │
├─────────────────────────────┤
│  [训练营信息]                 │
│  名称: 7 天早起营             │
│  时间: 2025-01-01 - 01-07   │
│  押金: ¥99                  │
├─────────────────────────────┤
│  [会员信息]                  │
│  星球昵称: 球球的副业探索路   │
│  星球 ID: 88455815452182    │
│  微信昵称: 小球球             │
│  手机号: 138****1234        │
├─────────────────────────────┤
│  [打卡信息]                  │
│  打卡天数: 7 / 7 天 ✓        │
│  完成率: 100%                │
│                             │
│  [打卡日历]                  │
│  ✓✓✓✓✓✓✓                    │
│                             │
│  [打卡详情列表]              │
│  2025-01-07  18:30 ✓        │
│  2025-01-06  19:15 ✓        │
│  ...                        │
├─────────────────────────────┤
│  [退款信息]                  │
│  退款金额: ¥99              │
│  退款方式: 企业微信原路退回  │
│  申请时间: 2025-01-08       │
│  退款状态: 待审核            │
├─────────────────────────────┤
│  [操作日志]                  │
│  2025-01-08 10:00           │
│  系统自动创建退款申请        │
│                             │
│  2025-01-08 10:05           │
│  管理员张三标记为待审核      │
├─────────────────────────────┤
│  [操作按钮]                  │
│  [审核通过] [审核拒绝]       │
└─────────────────────────────┘
```

**交互细节**：
- "审核通过"：
  - 弹出确认对话框
  - 显示退款金额和方式
  - 可选填写备注
  - 点击确认后调用企业微信退款接口
- "审核拒绝"：
  - 弹出对话框
  - 必须填写拒绝原因
  - 确认后更新状态并通知会员

---

#### 8. 数据统计页

**路由**：`/statistics`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] 数据统计                          │
│  [时间范围选择] 近 7 天 | 近 30 天 | 自定义  │
├─────────────────────────────────────────────┤
│  [关键指标卡片组]                             │
│  ┌──────┬──────┬──────┬──────┬──────┐      │
│  │总收入│总退款│ 净收入│完成率│退款率│      │
│  │125K │ 98K │ 27K  │ 85% │ 78% │      │
│  │+8K↑ │+6K↑ │+2K↑  │+2%↑│-1%↓ │      │
│  └──────┴──────┴──────┴──────┴──────┘      │
├─────────────────────────────────────────────┤
│  [图表区域]                                  │
│  ┌───────────────────────────────────────┐  │
│  │ 押金收入与退款趋势对比图 (双轴折线图)  │  │
│  │                                       │  │
│  │  ↑                                   │  │
│  │  │     ┌─ 押金收入                  │  │
│  │  │    /                             │  │
│  │  │   /    ┌── 退款金额              │  │
│  │  │  /    /                          │  │
│  │  │ /    /                           │  │
│  │  └────────────────────────→         │  │
│  │    1/1  1/5  1/10  1/15  1/20       │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  ┌─────────────────┬──────────────────────┐│
│  │ 训练营分类占比   │  打卡完成率分布       ││
│  │ (饼图)          │  (柱状图)            ││
│  │                │                      ││
│  │ 早起营 40%     │  0-60%:  5个         ││
│  │ 阅读营 30%     │  60-80%: 8个         ││
│  │ 写作营 20%     │  80-100%: 10个       ││
│  │ 其他   10%     │                      ││
│  └─────────────────┴──────────────────────┘│
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ 会员参与度排行榜 (Top 10)              │  │
│  │                                       │  │
│  │ 1. 球球的副业探索路  参加 5 次 完成 5 次││
│  │ 2. Aaron            参加 5 次 完成 4 次││
│  │ ...                                   │  │
│  └───────────────────────────────────────┘  │
├─────────────────────────────────────────────┤
│  [导出报表]                                  │
│  [导出 Excel] [导出 PDF] [定时推送]          │
└─────────────────────────────────────────────┘
```

**核心功能**：
- 多维度数据统计
- 数据可视化展示
- 报表导出
- 定时推送

**统计维度**：
- 时间维度：按日、周、月统计
- 训练营维度：按类型、状态统计
- 会员维度：活跃度、完成率排名
- 财务维度：收入、退款、净收入趋势

---

#### 9. SOP 教程管理页

**路由**：`/settings/tutorial`

**页面布局**：
```
┌─────────────────────────────────────────────┐
│  [页面标题] SOP 教程管理                      │
│  [操作按钮] [新增教程] [批量排序]            │
├─────────────────────────────────────────────┤
│  [分类列表]                                  │
│  ┌─────────┬────────────────────────┬─────┐│
│  │ 分类    │ 教程列表                │ 操作 ││
│  ├─────────┼────────────────────────┼─────┤│
│  │新手入门 │ 1. 如何注册登录          │编辑 ││
│  │ (展开)  │ 2. 如何浏览训练营        │删除 ││
│  │         │ 3. 如何选择合适的训练营  │排序 ││
│  │         │ [+新增]                 │     ││
│  ├─────────┼────────────────────────┼─────┤│
│  │支付押金 │ 1. 如何支付押金          │编辑 ││
│  │ (收起)  │ [展开查看更多]           │删除 ││
│  ├─────────┼────────────────────────┼─────┤│
│  │打卡指南 │ ...                     │     ││
│  └─────────┴────────────────────────┴─────┘│
└─────────────────────────────────────────────┘
```

**核心功能**：
- 教程分类管理
- 富文本编辑
- 拖拽排序
- 预览效果

**编辑教程弹窗**：
- 教程标题
- 所属分类
- 富文本内容编辑器
- 上传图片/视频
- 排序权重
- 是否显示

---

### Web 管理后台通用组件

#### 1. 数据卡片组件 (DataCard)

**Props**：
```typescript
interface DataCardProps {
  title: string;           // 标题
  value: number | string;  // 数值
  unit?: string;           // 单位
  trend?: {                // 趋势
    value: number;         // 变化值
    type: 'up' | 'down';   // 上升/下降
  };
  icon?: string;           // 图标
}
```

#### 2. 图表容器组件 (ChartContainer)

**Props**：
```typescript
interface ChartContainerProps {
  title: string;           // 图表标题
  loading?: boolean;       // 加载状态
  empty?: boolean;         // 空数据状态
  extra?: VNode;           // 右上角额外操作
}
```

#### 3. 数据表格组件 (DataTable)

**功能增强**：
- 列宽自适应
- 虚拟滚动（大数据量）
- 列设置（显示/隐藏）
- 导出 Excel
- 批量操作

#### 4. 搜索表单组件 (SearchForm)

**Props**：
```typescript
interface SearchFormProps {
  fields: FormField[];     // 搜索字段配置
  onSearch: (values) => void;  // 搜索回调
  onReset: () => void;     // 重置回调
}
```

---

## 前端技术实现细节

### 路由设计

#### H5 会员端路由表

```typescript
const routes = [
  {
    path: '/',
    redirect: '/camps'
  },
  {
    path: '/camps',
    name: 'CampList',
    component: () => import('@/views/CampList.vue'),
    meta: { title: '训练营', keepAlive: true }
  },
  {
    path: '/camps/:id',
    name: 'CampDetail',
    component: () => import('@/views/CampDetail.vue'),
    meta: { title: '训练营详情' }
  },
  {
    path: '/payment/:campId',
    name: 'Payment',
    component: () => import('@/views/Payment.vue'),
    meta: { title: '支付押金', requiresAuth: true }
  },
  {
    path: '/my-projects',
    name: 'MyProjects',
    component: () => import('@/views/MyProjects.vue'),
    meta: { title: '我的项目', requiresAuth: true, keepAlive: true }
  },
  {
    path: '/my-projects/:id',
    name: 'MyProjectDetail',
    component: () => import('@/views/MyProjectDetail.vue'),
    meta: { title: '项目详情', requiresAuth: true }
  },
  {
    path: '/tutorial',
    name: 'Tutorial',
    component: () => import('@/views/Tutorial.vue'),
    meta: { title: 'SOP 教程', keepAlive: true }
  },
  {
    path: '/profile',
    name: 'Profile',
    component: () => import('@/views/Profile.vue'),
    meta: { title: '个人中心', requiresAuth: true }
  }
];
```

#### Web 管理后台路由表

```typescript
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue'),
    meta: { title: '登录' }
  },
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/Dashboard.vue'),
        meta: { title: '工作台', icon: 'home', requiresAuth: true }
      }
    ]
  },
  {
    path: '/camps',
    component: Layout,
    meta: { title: '训练营管理', icon: 'camp' },
    children: [
      {
        path: '',
        name: 'CampList',
        component: () => import('@/views/camp/List.vue'),
        meta: { title: '训练营列表', requiresAuth: true }
      },
      {
        path: 'create',
        name: 'CampCreate',
        component: () => import('@/views/camp/Create.vue'),
        meta: { title: '新建训练营', requiresAuth: true }
      },
      {
        path: ':id/edit',
        name: 'CampEdit',
        component: () => import('@/views/camp/Edit.vue'),
        meta: { title: '编辑训练营', requiresAuth: true }
      }
    ]
  },
  {
    path: '/members',
    component: Layout,
    meta: { title: '会员管理', icon: 'user' },
    children: [
      {
        path: '',
        name: 'MemberList',
        component: () => import('@/views/member/List.vue'),
        meta: { title: '会员列表', requiresAuth: true }
      },
      {
        path: 'match',
        name: 'MemberMatch',
        component: () => import('@/views/member/Match.vue'),
        meta: { title: '会员匹配审核', requiresAuth: true }
      }
    ]
  },
  {
    path: '/refunds',
    component: Layout,
    meta: { title: '退款管理', icon: 'money' },
    children: [
      {
        path: '',
        name: 'RefundList',
        component: () => import('@/views/refund/List.vue'),
        meta: { title: '退款列表', requiresAuth: true }
      }
    ]
  },
  {
    path: '/statistics',
    component: Layout,
    children: [
      {
        path: '',
        name: 'Statistics',
        component: () => import('@/views/Statistics.vue'),
        meta: { title: '数据统计', icon: 'chart', requiresAuth: true }
      }
    ]
  },
  {
    path: '/settings',
    component: Layout,
    meta: { title: '系统设置', icon: 'setting' },
    children: [
      {
        path: 'basic',
        name: 'BasicSettings',
        component: () => import('@/views/settings/Basic.vue'),
        meta: { title: '基础配置', requiresAuth: true }
      },
      {
        path: 'tutorial',
        name: 'TutorialSettings',
        component: () => import('@/views/settings/Tutorial.vue'),
        meta: { title: 'SOP 教程管理', requiresAuth: true }
      },
      {
        path: 'permission',
        name: 'PermissionSettings',
        component: () => import('@/views/settings/Permission.vue'),
        meta: { title: '权限管理', requiresAuth: true }
      }
    ]
  }
];
```

---

### 状态管理设计

#### Pinia Store 结构

**H5 会员端 Stores**：

```typescript
// stores/user.ts
export const useUserStore = defineStore('user', {
  state: () => ({
    userInfo: null,           // 用户信息
    token: '',                // 登录 Token
    planetUserId: '',         // 知识星球用户 ID
    planetNickname: ''        // 知识星球昵称
  }),
  actions: {
    async login(),            // 登录
    async logout(),           // 登出
    async getUserInfo()       // 获取用户信息
  }
});

// stores/camp.ts
export const useCampStore = defineStore('camp', {
  state: () => ({
    campList: [],            // 训练营列表
    currentCamp: null,       // 当前查看的训练营
    filters: {}              // 筛选条件
  }),
  actions: {
    async fetchCampList(),   // 获取训练营列表
    async fetchCampDetail(id) // 获取训练营详情
  }
});

// stores/myProject.ts
export const useMyProjectStore = defineStore('myProject', {
  state: () => ({
    projectList: [],         // 我的项目列表
    currentProject: null     // 当前查看的项目
  }),
  actions: {
    async fetchMyProjects(), // 获取我的项目
    async fetchProjectDetail(id) // 获取项目详情
  }
});
```

**Web 管理后台 Stores**：

```typescript
// stores/admin/user.ts
export const useAdminUserStore = defineStore('adminUser', {
  state: () => ({
    userInfo: null,          // 管理员信息
    token: '',               // Token
    permissions: []          // 权限列表
  }),
  actions: {
    async login(),           // 登录
    async logout()           // 登出
  }
});

// stores/admin/camp.ts
export const useAdminCampStore = defineStore('adminCamp', {
  state: () => ({
    campList: [],            // 训练营列表
    total: 0,                // 总数
    pagination: {            // 分页
      page: 1,
      pageSize: 20
    }
  }),
  actions: {
    async fetchCampList(),   // 获取列表
    async createCamp(),      // 创建训练营
    async updateCamp(),      // 更新训练营
    async deleteCamp()       // 删除训练营
  }
});

// stores/admin/refund.ts
export const useAdminRefundStore = defineStore('adminRefund', {
  state: () => ({
    refundList: [],          // 退款列表
    pendingCount: 0          // 待处理数量
  }),
  actions: {
    async fetchRefundList(), // 获取退款列表
    async approveRefund(),   // 审核通过
    async rejectRefund()     // 审核拒绝
  }
});
```

---

### API 请求封装

#### Axios 实例配置

```typescript
// utils/request.ts
import axios from 'axios';
import { useUserStore } from '@/stores/user';
import { showToast } from 'vant'; // H5 端
// import { ElMessage } from 'element-plus'; // Web 端

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
});

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    const userStore = useUserStore();
    if (userStore.token) {
      config.headers.Authorization = `Bearer ${userStore.token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const { code, message, data } = response.data;
    if (code === 200) {
      return data;
    } else {
      showToast(message || '请求失败');
      return Promise.reject(new Error(message));
    }
  },
  (error) => {
    if (error.response) {
      const { status } = error.response;
      if (status === 401) {
        // Token 过期，跳转登录
        const userStore = useUserStore();
        userStore.logout();
      } else if (status === 403) {
        showToast('无权限访问');
      } else {
        showToast('网络错误，请稍后重试');
      }
    }
    return Promise.reject(error);
  }
);

export default request;
```

#### API 模块化管理

```typescript
// api/camp.ts
import request from '@/utils/request';

export const campApi = {
  // 获取训练营列表
  getCampList: (params) => {
    return request.get('/api/h5/camps', { params });
  },

  // 获取训练营详情
  getCampDetail: (id) => {
    return request.get(`/api/h5/camps/${id}`);
  },

  // 报名训练营
  enrollCamp: (campId, data) => {
    return request.post(`/api/h5/camps/${campId}/enroll`, data);
  },

  // 获取打卡排行榜
  getRankingList: (campId) => {
    return request.get(`/api/h5/camps/${campId}/ranking`);
  }
};

// api/payment.ts
export const paymentApi = {
  // 创建支付订单
  createOrder: (data) => {
    return request.post('/api/h5/payment/create', data);
  },

  // 查询支付状态
  queryPaymentStatus: (orderId) => {
    return request.get(`/api/h5/payment/${orderId}/status`);
  }
};

// api/myProject.ts
export const myProjectApi = {
  // 获取我的项目列表
  getMyProjects: (params) => {
    return request.get('/api/h5/my-projects', { params });
  },

  // 获取项目详情
  getProjectDetail: (id) => {
    return request.get(`/api/h5/my-projects/${id}`);
  },

  // 获取打卡记录
  getCheckInRecords: (projectId) => {
    return request.get(`/api/h5/my-projects/${projectId}/checkins`);
  }
};
```

---

### 性能优化方案

#### 1. 代码分割

```typescript
// 路由懒加载
const routes = [
  {
    path: '/camps/:id',
    component: () => import(/* webpackChunkName: "camp-detail" */ '@/views/CampDetail.vue')
  }
];

// 组件异步加载
const AsyncComponent = defineAsyncComponent({
  loader: () => import('@/components/HeavyComponent.vue'),
  loadingComponent: LoadingSpinner,
  errorComponent: ErrorComponent,
  delay: 200,
  timeout: 3000
});
```

#### 2. 图片优化

```typescript
// 使用 WebP 格式
<img
  :src="`${imageUrl}?x-oss-process=image/format,webp`"
  loading="lazy"
  alt="训练营封面"
/>

// 响应式图片
<picture>
  <source
    :srcset="`${imageUrl}?x-oss-process=image/resize,w_750`"
    media="(max-width: 375px)"
  />
  <source
    :srcset="`${imageUrl}?x-oss-process=image/resize,w_1125`"
    media="(max-width: 750px)"
  />
  <img :src="imageUrl" alt="训练营封面" />
</picture>
```

#### 3. 虚拟列表

```vue
<!-- 使用 Vant List 组件实现虚拟滚动 -->
<van-list
  v-model:loading="loading"
  :finished="finished"
  finished-text="没有更多了"
  @load="onLoad"
>
  <camp-card
    v-for="camp in campList"
    :key="camp.id"
    :camp="camp"
  />
</van-list>
```

#### 4. 缓存策略

```typescript
// 使用 keep-alive 缓存路由组件
<router-view v-slot="{ Component }">
  <keep-alive :include="cachedViews">
    <component :is="Component" />
  </keep-alive>
</router-view>

// API 请求缓存
const cache = new Map();
export const getCampListWithCache = async (params) => {
  const key = JSON.stringify(params);
  if (cache.has(key)) {
    return cache.get(key);
  }
  const data = await campApi.getCampList(params);
  cache.set(key, data);
  return data;
};
```

---

### 响应式设计方案

#### H5 端适配

```scss
// 使用 vw 单位
$base-width: 375px;

@function vw($px) {
  @return ($px / $base-width) * 100vw;
}

.camp-card {
  width: vw(343px);  // 343px / 375px = 91.47vw
  padding: vw(16px);
  margin-bottom: vw(12px);
}

// 使用 rem 单位
html {
  font-size: calc(100vw / 37.5); // 1rem = 10px (在 375px 宽度下)
}

.title {
  font-size: 1.6rem; // 16px
}
```

#### Web 端响应式

```scss
// 使用媒体查询
@media (max-width: 1024px) {
  .dashboard-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 768px) {
  .dashboard-grid {
    grid-template-columns: 1fr;
  }
}
```

---

### 错误处理和日志

```typescript
// 全局错误处理
app.config.errorHandler = (err, instance, info) => {
  console.error('Global error:', err, info);
  // 上报错误到监控平台
  reportError(err, {
    componentName: instance?.$options.name,
    info
  });
};

// 日志工具
export const logger = {
  info: (message, data) => {
    console.log(`[INFO] ${message}`, data);
  },
  warn: (message, data) => {
    console.warn(`[WARN] ${message}`, data);
  },
  error: (message, error) => {
    console.error(`[ERROR] ${message}`, error);
    // 上报错误
    reportError(error, { message });
  }
};
```

---

## 交互设计规范

### 加载状态

- **全局加载**：使用 Loading 组件覆盖整个页面
- **局部加载**：使用 Skeleton 骨架屏
- **按钮加载**：按钮显示 Loading 图标，禁用点击

### 空状态

- **无数据**：显示友好的空状态提示 + 引导操作
- **网络错误**：显示错误图标 + 重试按钮
- **权限不足**：显示权限提示 + 返回按钮

### 反馈提示

- **成功提示**：绿色 Toast，1.5 秒自动消失
- **错误提示**：红色 Toast，3 秒自动消失
- **警告提示**：橙色 Toast，2 秒自动消失
- **确认对话框**：重要操作前弹出确认框

### 表单验证

- **实时验证**：输入框失焦时验证
- **提交验证**：提交前统一验证
- **错误提示**：在输入框下方显示红色错误信息

---

## 可访问性 (Accessibility)

### 语义化 HTML

```html
<!-- 使用语义化标签 -->
<header>导航栏</header>
<main>主内容</main>
<footer>底部信息</footer>

<!-- 使用 ARIA 属性 -->
<button aria-label="关闭">×</button>
<div role="dialog" aria-labelledby="modal-title">...</div>
```

### 键盘导航

- 确保所有交互元素可通过 Tab 键访问
- 使用 Enter/Space 键触发按钮
- 使用 Esc 键关闭弹窗

### 颜色对比度

- 文字与背景对比度至少 4.5:1
- 重要信息不仅依赖颜色区分

---

## 浏览器兼容性

### H5 会员端

- **移动浏览器**：
  - iOS Safari 13+
  - Android Chrome 90+
  - 微信内置浏览器
  - 企业微信内置浏览器

### Web 管理后台

- **桌面浏览器**：
  - Chrome 90+
  - Edge 90+
  - Firefox 88+
  - Safari 14+

---

## 部署和构建

### 环境变量配置

```env
# H5 会员端 (.env.production)
VITE_API_BASE_URL=https://api.example.com
VITE_APP_TITLE=知识星球训练营
VITE_WECHAT_APPID=wx1234567890

# Web 管理后台 (.env.production)
VITE_API_BASE_URL=https://admin-api.example.com
VITE_APP_TITLE=训练营管理后台
```

### 构建命令

```bash
# H5 会员端
cd frontend/h5-member
pnpm install
pnpm run build

# Web 管理后台
cd frontend/admin-web
pnpm install
pnpm run build
```

### Nginx 配置

```nginx
# H5 会员端
server {
  listen 80;
  server_name h5.example.com;

  root /var/www/h5-member/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api {
    proxy_pass http://backend:8080;
  }
}

# Web 管理后台
server {
  listen 80;
  server_name admin.example.com;

  root /var/www/admin-web/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api {
    proxy_pass http://backend:8080;
  }
}
```

---

## 开发规范

### 组件命名

- **PascalCase**：组件文件名和组件名 (`CampCard.vue`, `MyProjectList.vue`)
- **kebab-case**：模板中使用组件 (`<camp-card />`, `<my-project-list />`)

### 文件组织

```
src/
├── api/              # API 请求模块
│   ├── camp.ts
│   └── payment.ts
├── assets/           # 静态资源
│   ├── images/
│   └── styles/
├── components/       # 通用组件
│   ├── CampCard.vue
│   └── StatusTag.vue
├── composables/      # 组合式函数
│   ├── useCampList.ts
│   └── usePayment.ts
├── layouts/          # 布局组件
│   └── DefaultLayout.vue
├── router/           # 路由配置
│   └── index.ts
├── stores/           # 状态管理
│   ├── user.ts
│   └── camp.ts
├── utils/            # 工具函数
│   ├── request.ts
│   └── format.ts
├── views/            # 页面组件
│   ├── CampList.vue
│   └── Payment.vue
├── App.vue           # 根组件
└── main.ts           # 入口文件
```

### 代码注释

```typescript
/**
 * 获取训练营列表
 * @param params - 查询参数
 * @param params.status - 训练营状态 (ongoing | upcoming | ended)
 * @param params.keyword - 搜索关键词
 * @returns 训练营列表数据
 */
export const getCampList = async (params: CampListParams) => {
  // 实现代码
};
```

---

## 测试策略

### 单元测试

```typescript
// 使用 Vitest
import { describe, it, expect } from 'vitest';
import { formatDate } from '@/utils/format';

describe('formatDate', () => {
  it('should format date correctly', () => {
    const date = new Date('2025-01-01 00:00:00');
    expect(formatDate(date, 'YYYY-MM-DD')).toBe('2025-01-01');
  });
});
```

### 组件测试

```typescript
// 使用 @vue/test-utils
import { mount } from '@vue/test-utils';
import CampCard from '@/components/CampCard.vue';

describe('CampCard', () => {
  it('renders correctly', () => {
    const camp = {
      id: '1',
      title: '7天早起营',
      status: 'ongoing'
    };
    const wrapper = mount(CampCard, {
      props: { camp }
    });
    expect(wrapper.text()).toContain('7天早起营');
  });
});
```

### E2E 测试

```typescript
// 使用 Playwright
import { test, expect } from '@playwright/test';

test('user can enroll in a camp', async ({ page }) => {
  await page.goto('http://localhost:5173');
  await page.click('text=7天早起营');
  await page.click('text=立即报名');
  await page.fill('input[name="planetNickname"]', '测试用户');
  await page.click('text=确认支付');
  await expect(page.locator('text=支付成功')).toBeVisible();
});
```

---

## 总结

本文档详细描述了知识星球训练营自动退款系统 V1 版本的 UI/UX 设计和前端实现方案，包括：

1. **H5 会员端**：7 个核心页面 + 4 个通用组件
2. **Web 管理后台**：9 个核心页面 + 4 个通用组件
3. **技术栈选型**：Vue 3 + Vite + Pinia + Vant/Element Plus
4. **前端架构**：路由设计、状态管理、API 封装
5. **性能优化**：代码分割、图片优化、虚拟列表、缓存策略
6. **开发规范**：组件命名、文件组织、代码注释
7. **测试策略**：单元测试、组件测试、E2E 测试

**下一步**：
- 根据本文档创建前端项目骨架
- 实现核心页面和组件
- 对接后端 API
- 进行测试和优化