# V1 版本 UI/UX 和前端设计提示词

## 文档信息

- **版本**：v1.1
- **更新日期**：2025-10-24
- **关联文档**：
  - [产品需求文档 (PRD)](../../docs/PRD.md)
  - [技术架构设计](../../docs/v1/技术架构设计.md)

---

## 设计目标

基于知识星球训练营自动退款系统的业务需求，设计简洁、高效、用户友好的前端界面，满足会员端和管理端的不同使用场景。

**核心设计原则**：

- **简洁优先**：减少认知负担，核心功能一目了然
- **移动优先**：H5 端优先考虑移动端体验
- **高效操作**：减少操作步骤，提升管理效率
- **数据可视化**：关键数据用图表直观展示
- **一致性**：统一的设计语言和交互规范

---

## 技术栈选型

> 与技术架构设计文档保持一致

### H5 会员端

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 | 3.3+ |
| UI 组件库 | Vant | 4.x |
| 构建工具 | Vite | 5.x |
| 路由 | Vue Router | 4.x |
| 状态管理 | Pinia | 2.x |
| HTTP 客户端 | Axios | 1.x |
| 样式预处理 | Less/Sass | - |
| 日期处理 | Day.js | - |

### Web 管理后台

| 类别 | 技术 | 版本 |
|------|------|------|
| 框架 | Vue 3 | 3.3+ |
| UI 组件库 | Element Plus | 2.4+ |
| 构建工具 | Vite | 5.x |
| 路由 | Vue Router | 4.x |
| 状态管理 | Pinia | 2.x |
| HTTP 客户端 | Axios | 1.x |
| 图表库 | ECharts | 5.x |
| Excel 处理 | XLSX | - |

### 共享基础设施

- **代码规范**：ESLint + Prettier
- **Git Hooks**：Husky + lint-staged
- **包管理器**：pnpm
- **环境变量**：.env 文件管理

---

## 一、H5 会员端设计

### 1.1 整体布局

**设计风格**：

- 清新简洁，符合知识付费产品调性
- 主色调：蓝色系 (#1989FA - Vant 主题色)
- 辅助色：成功绿 (#07C160)、警告橙 (#FF976A)、错误红 (#EE0A24)
- 背景色：浅灰 (#F7F8FA)，卡片白色 (#FFFFFF)

**导航结构**：

H5 端采用**扁平化页面结构**，无底部 Tab 导航，通过链接/二维码直接进入：

```
入口：扫描知识星球帖子中的二维码/链接
  ├── 训练营列表页 ──→ 训练营详情页 ──→ 报名支付页 ──→ 群二维码展示页
  └── 打卡进度查询页（通过链接直接访问）
```

**适配方案**：

- 使用 `viewport` 单位 (vw/vh) 实现响应式
- 字体大小使用 `rem`，基于设计稿 375px 宽度
- 关键触控区域最小 44px × 44px

---

### 1.2 页面设计

#### 1.2.1 训练营列表页

**路由**：`/` 或 `/camps`

**页面布局**：

```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  知识星球训练营               │
├─────────────────────────────┤
│  [状态筛选 Tab]              │
│  进行中 | 即将开始            │
├─────────────────────────────┤
│  [训练营卡片 1]              │
│  ┌───────────────────────┐  │
│  │ [海报图]    [状态标签] │  │
│  │ 训练营名称             │  │
│  │ 时间: 01-01 ~ 01-21   │  │
│  │ 押金: ¥99  要求: 15/21│  │
│  │ 已报名 50人            │  │
│  │ [立即报名] 按钮        │  │
│  └───────────────────────┘  │
│                             │
│  [训练营卡片 2]              │
│  ...                        │
│                             │
│  [下拉刷新 / 加载更多]        │
└─────────────────────────────┘
```

**核心功能**：

- 下拉刷新，上拉加载更多
- 状态筛选：进行中、即将开始（不显示已结束）
- 显示打卡要求（如：21天15打卡）

**交互细节**：

- 卡片点击进入报名页
- 状态标签颜色：进行中（绿）、即将开始（蓝）
- 已报名显示灰色"已报名"文字

**数据结构** (对应后端 CampVO)：

```typescript
interface Camp {
  id: number;
  name: string;                    // 训练营名称
  posterUrl: string;               // 海报图
  deposit: number;                 // 押金金额
  startDate: string;               // 开始日期
  endDate: string;                 // 结束日期
  totalDays: number;               // 总天数
  requiredDays: number;            // 要求打卡天数
  enrollCount: number;             // 已报名人数
  status: 'NOT_STARTED' | 'ONGOING' | 'ENDED';
}
```

---

#### 1.2.2 报名支付页

**路由**：`/enroll/:campId`

**页面布局**：

```
┌─────────────────────────────┐
│  [顶部导航栏] [返回]          │
│  报名参加                     │
├─────────────────────────────┤
│  [训练营信息卡片]             │
│  ┌───────────────────────┐  │
│  │ [海报缩略图]           │  │
│  │ 训练营名称             │  │
│  │ 时间: 01-01 ~ 01-21   │  │
│  │ 打卡要求: 21天15打卡   │  │
│  │ 押金: ¥99             │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  [信息填写表单]              │
│  ┌───────────────────────┐  │
│  │ 星球昵称 *             │  │
│  │ [输入框]               │  │
│  │                        │  │
│  │ 星球ID *               │  │
│  │ [输入框]               │  │
│  │ 💡 如何获取星球ID？     │  │
│  │                        │  │
│  │ 微信昵称 *             │  │
│  │ [输入框]               │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  [退款规则说明]              │
│  ● 完成打卡要求后自动退款     │
│  ● 项目结束后3个工作日内到账 │
│  ● 原路退回                  │
├─────────────────────────────┤
│  [底部操作栏]                │
│  [支付押金 ¥99]              │
└─────────────────────────────┘
```

**核心功能**：

- 展示训练营详情
- 填写报名信息（星球昵称、星球ID、微信昵称）
- 调起企业微信支付

**交互细节**：

- 表单校验：所有字段必填
- 星球ID 提供获取说明（弹窗或跳转）
- 支付按钮防重复点击（Loading 状态）
- 同一会员同一训练营只能支付一次

**业务规则**（对应 PRD 9.1）：

- 重复支付时提示"您已报名"，显示群二维码

---

#### 1.2.3 群二维码展示页

**路由**：`/qrcode/:paymentId`

**页面布局**：

```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  报名成功                     │
├─────────────────────────────┤
│                             │
│      ✅ 支付成功！           │
│                             │
│  ┌───────────────────────┐  │
│  │                       │  │
│  │     [群二维码大图]      │  │
│  │                       │  │
│  │                       │  │
│  └───────────────────────┘  │
│                             │
│  请长按保存二维码            │
│  扫码加入训练营学习群        │
│                             │
├─────────────────────────────┤
│  [查看我的打卡进度]          │
└─────────────────────────────┘
```

**核心功能**：

- 显示群二维码（大图，可长按保存）
- 提供进入打卡进度页的入口

---

#### 1.2.4 打卡进度查询页

**路由**：`/progress/:memberId` 或 `/progress?planetUserId=xxx&campId=xxx`

**页面布局**：

```
┌─────────────────────────────┐
│  [顶部导航栏]                 │
│  我的打卡进度                 │
├─────────────────────────────┤
│  [训练营信息]                │
│  训练营名称                  │
│  时间进度: 已进行 5天/共21天 │
│  ────────────●───── 24%    │
├─────────────────────────────┤
│  [打卡统计]                  │
│  ┌─────────────────────┐   │
│  │ 已打卡     要求     状态│   │
│  │   5天    / 15天    ✓达标│   │
│  │                     │   │
│  │ 进度条 ████░░░░ 33% │   │
│  └─────────────────────┘   │
├─────────────────────────────┤
│  [打卡日历]                  │
│  一 二 三 四 五 六 日        │
│  ✓  ✓  ✗  ✓  ✓  ○  ○       │
│  1  2  3  4  5  6  7        │
│                             │
│  ✓已打卡  ✗未打卡  ○未开始  │
├─────────────────────────────┤
│  [退款状态]                  │
│  状态: 进行中 / 待审核 / 已退款│
│  押金: ¥99                  │
│  (已退款时显示退款时间)       │
├─────────────────────────────┤
│  [打卡记录列表]              │
│  01-05  18:30 ✓ 已打卡      │
│  01-04  19:15 ✓ 已打卡      │
│  01-03  --:-- ✗ 未打卡      │
│  ...                        │
└─────────────────────────────┘
```

**核心功能**：

- 时间进度可视化
- 打卡统计和进度条
- 打卡日历展示
- 退款状态追踪

**交互细节**：

- 实时显示最新打卡数据（每天01:00后更新）
- 支持下拉刷新
- 退款状态颜色：待审核（橙）、已退款（绿）、不符合条件（灰）

---

### 1.3 H5 端通用组件

#### 训练营卡片 (CampCard)

```typescript
interface CampCardProps {
  camp: Camp;
  showEnrollButton?: boolean;  // 是否显示报名按钮
}
```

#### 状态标签 (StatusTag)

```typescript
type StatusType =
  | 'ongoing'      // 进行中 - 绿色
  | 'upcoming'     // 即将开始 - 蓝色
  | 'ended'        // 已结束 - 灰色
  | 'qualified'    // 达标 - 绿色
  | 'unqualified'; // 未达标 - 红色
```

#### 打卡日历 (CheckinCalendar)

```typescript
interface CheckinCalendarProps {
  startDate: string;
  endDate: string;
  checkinDates: string[];  // 已打卡日期列表
}
```

#### 进度条 (ProgressBar)

```typescript
interface ProgressBarProps {
  current: number;
  total: number;
  showLabel?: boolean;
  color?: 'green' | 'orange' | 'red';  // 根据进度自动变色
}
```

---

## 二、Web 管理后台设计

### 2.1 整体布局

**设计风格**：

- 专业、高效的企业级后台风格
- 主色调：蓝色系 (#409EFF - Element Plus 主题色)
- 采用侧边栏 + 顶栏布局
- 响应式设计（支持桌面端和平板）

**布局结构**：

```
┌──────────┬─────────────────────────────────┐
│          │  [顶部导航栏]                     │
│          │  [面包屑] [用户信息] [退出]       │
│  [侧边栏] ├─────────────────────────────────┤
│          │                                  │
│  Logo    │                                  │
│          │        [主内容区域]              │
│  菜单     │                                  │
│          │                                  │
│          │                                  │
│          │                                  │
└──────────┴─────────────────────────────────┘
```

**侧边栏菜单** (与 PRD 权限矩阵对齐)：

```
📊 工作台 (Dashboard)
🏕️ 训练营管理
    ├── 训练营列表
    └── 创建训练营
👥 会员管理
    ├── 会员列表
    └── 会员详情
📝 打卡管理
    ├── 打卡记录
    └── 打卡进度统计
💰 退款审核
    ├── 待审核列表
    └── 退款记录
📈 统计报表
    └── 训练营报表
⚙️ 系统管理 (仅超级管理员)
    ├── 用户管理
    ├── 系统配置
    └── 操作日志
```

---

### 2.2 角色权限设计

> 与 PRD 2.2 权限矩阵对齐

| 功能模块 | 超级管理员 | 管理员 | 教练 | 志愿者 |
|---------|-----------|--------|------|--------|
| 训练营管理 | ✅ 全部 | ✅ 全部 | 👁️ 查看负责项目 | ❌ |
| 会员管理 | ✅ 全部 | ✅ 全部 | 👁️ 查看负责项目 | 👁️ 查看负责项目 |
| 打卡管理 | ✅ 全部 | ✅ 全部 | 👁️ 查看负责项目 | 👁️ 查看负责项目 |
| 退款审核 | ✅ 全部 | ✅ 全部 | ✅ 审核 | ❌ |
| 统计报表 | ✅ 全部 | ✅ 全部 | 👁️ 查看负责项目 | ❌ |
| 系统管理 | ✅ 全部 | ❌ | ❌ | ❌ |

---

### 2.3 页面设计

#### 2.3.1 工作台 (Dashboard)

**路由**：`/dashboard`

**页面布局**：

```
┌─────────────────────────────────────────────┐
│  [数据概览卡片组]                             │
│  ┌──────────┬──────────┬──────────┬─────┐  │
│  │ 总训练营 │ 进行中   │ 总会员   │净收入│  │
│  │   156   │   23    │  3,542  │ 27K │  │
│  │  +12↑   │         │  +89↑   │+2K↑ │  │
│  └──────────┴──────────┴──────────┴─────┘  │
├─────────────────────────────────────────────┤
│  [待办事项]                                  │
│  ● 5 个训练营即将结束，需要处理退款           │
│  ● 12 个会员匹配待确认                       │
│  ● 3 个退款失败需要处理                      │
├─────────────────────────────────────────────┤
│  [图表区域]                                  │
│  ┌─────────────────┬──────────────────────┐│
│  │ 近7天收入趋势    │  打卡完成率分布       ││
│  │ (柱状图)        │  (饼图)              ││
│  └─────────────────┴──────────────────────┘│
├─────────────────────────────────────────────┤
│  [快捷操作]                                  │
│  [新建训练营] [处理退款] [查看报表]          │
└─────────────────────────────────────────────┘
```

---

#### 2.3.2 训练营列表页

**路由**：`/camps`

**页面布局**：

```
┌─────────────────────────────────────────────┐
│  [页面标题] 训练营管理                        │
│  [+ 创建训练营]                              │
├─────────────────────────────────────────────┤
│  [搜索筛选区域]                               │
│  名称: [输入框]  状态: [全部▼]  时间: [日期]  │
│  [搜索] [重置]                               │
├─────────────────────────────────────────────┤
│  [数据表格]                                  │
│  ┌────────┬──────┬──────┬──────┬──────┬───┐│
│  │训练营   │ 押金 │ 时间 │ 报名 │ 状态 │操作││
│  ├────────┼──────┼──────┼──────┼──────┼───┤│
│  │7天早起营│ ¥99 │01-01 │ 50  │进行中│查看││
│  │        │      │01-07 │ /100│      │编辑││
│  │        │      │      │     │      │... ││
│  └────────┴──────┴──────┴──────┴──────┴───┘│
│                                             │
│  [分页] 共 156 条  < 1 2 3 ... >            │
└─────────────────────────────────────────────┘
```

**表格列定义**：

| 列 | 说明 | 操作 |
|---|---|---|
| 训练营名称 | 可点击查看详情 | - |
| 押金金额 | 显示金额 | - |
| 时间范围 | 开始~结束日期 | - |
| 打卡要求 | X天Y打卡 | - |
| 报名人数 | 已报名/最大人数 | - |
| 状态 | 未开始/进行中/已结束 | 标签颜色区分 |
| 操作 | 查看/编辑/生成二维码 | 下拉菜单 |

**业务规则**（对应 PRD 9.4）：

- 已开始的训练营不能删除
- 未开始的训练营可以编辑
- 进行中的训练营不能修改核心参数（日期、打卡要求）

---

#### 2.3.3 创建/编辑训练营页

**路由**：`/camps/create` 或 `/camps/:id/edit`

**表单字段** (对应 PRD 4.2.1.2)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 训练营名称 | 文本 | ✅ | 最多50字 |
| 项目海报 | 图片上传 | ✅ | jpg/png，最大2MB |
| 押金金额 | 数字 | ✅ | 单位：元 |
| 开始日期 | 日期选择 | ✅ | 不能早于今天 |
| 结束日期 | 日期选择 | ✅ | 必须晚于开始日期 |
| 打卡要求 | 组合输入 | ✅ | 总天数X，要求Y天 (Y≤X) |
| 群二维码 | 图片上传 | ✅ | jpg/png |
| 项目介绍 | 富文本 | ❌ | 最多500字 |
| 项目教练 | 多选 | ✅ | 从用户列表选择 |
| 志愿者 | 多选 | ❌ | 从用户列表选择 |
| 知识星球项目ID | 文本 | ✅ | 关联打卡项目 |
| 企业微信收款码ID | 文本 | ✅ | 用于识别支付记录 |

---

#### 2.3.4 训练营详情页

**路由**：`/camps/:id`

**Tab 切换**：概览 | 报名列表 | 打卡情况 | 退款情况

**Tab1：概览**

- 训练营基本信息
- 数据统计卡片（报名人数、支付总额、平均打卡率、退款总额）
- H5报名链接和二维码

**Tab2：报名列表** (对应 PRD 4.2.1.3)

| 列 | 说明 |
|---|---|
| 星球昵称 | - |
| 星球ID | - |
| 微信昵称 | - |
| 支付时间 | - |
| 支付金额 | - |
| 匹配状态 | 已匹配/待匹配/匹配失败 |
| 进群状态 | 已进群/未进群 |
| 操作 | 手动匹配/标记进群 |

**Tab3：打卡情况**

| 列 | 说明 |
|---|---|
| 会员信息 | 星球昵称 |
| 已打卡/要求 | X/Y天 |
| 完成率 | 百分比 |
| 是否达标 | ✅/❌ |
| 最后打卡时间 | - |

**Tab4：退款情况**

| 列 | 说明 |
|---|---|
| 会员信息 | 星球昵称 |
| 打卡完成 | X/Y天 |
| 退款金额 | ¥XX |
| 审核状态 | 待审核/已通过/已拒绝 |
| 退款状态 | 退款中/已退款/退款失败 |
| 操作时间 | - |

---

#### 2.3.5 会员管理页

**路由**：`/members`

**页面布局**：

```
┌─────────────────────────────────────────────┐
│  [页面标题] 会员管理                          │
├─────────────────────────────────────────────┤
│  [搜索筛选]                                  │
│  训练营: [选择▼]  匹配状态: [全部▼]           │
│  关键词: [输入框]  [搜索] [重置]              │
├─────────────────────────────────────────────┤
│  [数据表格]                                  │
│  ┌────────┬────────┬──────┬──────┬─────────┐│
│  │星球昵称│ 星球ID  │微信昵称│匹配状态│ 操作  ││
│  ├────────┼────────┼──────┼──────┼─────────┤│
│  │球球的副│88455815│小球球 │已匹配 │查看详情││
│  │业探索  │        │      │100%  │手动匹配││
│  └────────┴────────┴──────┴──────┴─────────┘│
└─────────────────────────────────────────────┘
```

**匹配置信度说明** (对应 PRD 7.1)：

- 100%：星球ID 完全匹配
- 66%：星球昵称匹配
- 33%：微信昵称匹配
- 匹配失败：无法自动匹配

---

#### 2.3.6 退款审核页

**路由**：`/refunds`

**Tab 切换**：待审核 | 退款记录

**待审核列表**：

```
┌─────────────────────────────────────────────┐
│  [页面标题] 退款审核                          │
│  [批量通过] [批量拒绝]                        │
├─────────────────────────────────────────────┤
│  [筛选] 训练营: [选择▼]  匹配置信度: [全部▼] │
├─────────────────────────────────────────────┤
│  [数据表格]                                  │
│  ┌──┬────────┬────┬─────┬──────┬──────┬───┐│
│  │☑│ 训练营  │会员│打卡 │ 押金 │匹配度│操作││
│  ├──┼────────┼────┼─────┼──────┼──────┼───┤│
│  │☑│早起营  │球球│ 7/7 │ ¥99 │ 100%│通过││
│  │ │        │    │ ✓达标│     │      │拒绝││
│  │ │        │    │     │     │      │详情││
│  └──┴────────┴────┴─────┴──────┴──────┴───┘│
└─────────────────────────────────────────────┘
```

**退款详情抽屉**：

- 训练营信息
- 会员信息（星球昵称、ID、微信昵称）
- 打卡信息（打卡天数、日历视图、详情列表）
- 退款信息（金额、方式、状态）
- 操作日志
- 操作按钮（审核通过/审核拒绝）

**状态流转** (对应 PRD 3.3)：

```
待审核 ──→ 审核通过 ──→ 退款中 ──→ 已退款
   │                      │
   └──→ 审核拒绝          └──→ 退款失败 ──→ 重试
```

---

#### 2.3.7 统计报表页

**路由**：`/statistics`

**页面布局** (对应 PRD 4.2.5)：

```
┌─────────────────────────────────────────────┐
│  [时间选择] 本月 | 上月 | 自定义              │
├─────────────────────────────────────────────┤
│  [总体数据卡片]                              │
│  ┌──────┬──────┬──────┬──────┬──────┐      │
│  │训练营│参与人│押金收入│退款额│ 净收入│      │
│  │ 总数 │ 总数 │       │     │      │      │
│  └──────┴──────┴──────┴──────┴──────┘      │
├─────────────────────────────────────────────┤
│  [训练营明细表]                              │
│  ┌────────┬──────┬──────┬──────┬──────┬───┐│
│  │训练营  │报名数│完成率│押金收入│退款额│净收入││
│  ├────────┼──────┼──────┼──────┼──────┼───┤│
│  │早起营  │ 100 │ 85% │¥9,900│¥8,415│¥1,485│
│  └────────┴──────┴──────┴──────┴──────┴───┘│
├─────────────────────────────────────────────┤
│  [图表区域]                                  │
│  ● 月度趋势图（参与人数、完成率）             │
│  ● 训练营对比图                              │
└─────────────────────────────────────────────┘
```

---

#### 2.3.8 系统管理页

> 仅超级管理员可访问

**用户管理** (对应 PRD 4.2.6.1)：

| 列 | 说明 |
|---|---|
| 用户名 | - |
| 角色 | 超级管理员/管理员/教练/志愿者 |
| 创建时间 | - |
| 最后登录时间 | - |
| 状态 | 启用/禁用 |
| 操作 | 编辑/删除/重置密码 |

**系统配置** (对应 PRD 4.2.6.2)：

- 知识星球配置（Cookie，加密存储）
- 企业微信配置（企业ID、Secret、商户号等）
- 通知配置（开关、接收人）
- 各配置提供 API 测试按钮

**操作日志** (对应 PRD 4.2.6.3)：

| 列 | 说明 |
|---|---|
| 时间 | - |
| 操作人 | - |
| 操作类型 | 创建训练营/审核退款/手动匹配等 |
| 操作对象 | 训练营名称/会员昵称等 |
| 操作详情 | 具体内容 |
| IP地址 | - |

---

### 2.4 管理后台通用组件

#### 数据卡片 (DataCard)

```typescript
interface DataCardProps {
  title: string;
  value: number | string;
  unit?: string;
  trend?: {
    value: number;
    type: 'up' | 'down';
  };
  icon?: string;
}
```

#### 搜索表单 (SearchForm)

```typescript
interface SearchFormProps {
  fields: FormField[];
  onSearch: (values: Record<string, any>) => void;
  onReset: () => void;
}
```

#### 数据表格 (DataTable)

功能增强：
- 列宽自适应
- 列设置（显示/隐藏）
- 导出 Excel
- 批量选择和操作

#### 详情抽屉 (DetailDrawer)

```typescript
interface DetailDrawerProps {
  visible: boolean;
  title: string;
  width?: string | number;
  onClose: () => void;
}
```

---

## 三、API 接口设计

> 与技术架构设计文档保持一致

### 3.1 统一响应格式

```json
{
  "code": 200,
  "message": "成功",
  "data": {},
  "timestamp": 1234567890
}
```

### 3.2 H5 端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 训练营列表 | GET | `/api/h5/camps` | 获取可报名的训练营 |
| 训练营详情 | GET | `/api/h5/camps/{campId}` | 获取训练营详情 |
| 创建支付 | POST | `/api/h5/payments` | 创建支付订单 |
| 获取支付参数 | GET | `/api/h5/payments/{paymentId}/params` | 获取企微支付参数 |
| 获取群二维码 | GET | `/api/h5/payments/{paymentId}/qrcode` | 获取群二维码 |
| 查询打卡进度 | GET | `/api/h5/progress/{memberId}` | 查询会员打卡进度 |

### 3.3 管理端接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 训练营列表 | GET | `/api/admin/camps` | 分页查询 |
| 创建训练营 | POST | `/api/admin/camps` | 创建 |
| 训练营详情 | GET | `/api/admin/camps/{campId}` | 详情 |
| 更新训练营 | PUT | `/api/admin/camps/{campId}` | 更新 |
| 生成报名链接 | GET | `/api/admin/camps/{campId}/enroll-url` | 生成H5链接 |
| 会员列表 | GET | `/api/admin/members` | 分页查询 |
| 会员详情 | GET | `/api/admin/members/{memberId}` | 详情 |
| 手动匹配 | POST | `/api/admin/members/{memberId}/match` | 手动匹配 |
| 标记进群 | POST | `/api/admin/members/{memberId}/mark-joined` | 标记已进群 |
| 待审核列表 | GET | `/api/admin/refunds/pending` | 待审核退款 |
| 审核通过 | POST | `/api/admin/refunds/{refundId}/approve` | 通过 |
| 批量审核 | POST | `/api/admin/refunds/batch-approve` | 批量通过 |
| 重试退款 | POST | `/api/admin/refunds/{refundId}/retry` | 重试失败的退款 |

---

## 四、交互规范

### 4.1 加载状态

- **页面加载**：显示 Loading 组件或骨架屏
- **按钮操作**：按钮显示 Loading 图标，禁用重复点击
- **列表加载**：显示 Loading 遮罩

### 4.2 空状态

- **无数据**：显示空状态插图 + 引导操作
- **网络错误**：显示错误提示 + 重试按钮
- **无权限**：显示权限提示

### 4.3 反馈提示

- **成功**：绿色 Toast，1.5s 自动消失
- **错误**：红色 Toast，3s 自动消失
- **确认操作**：弹出确认对话框

### 4.4 表单验证

- **实时验证**：输入框失焦时验证
- **提交验证**：提交前统一验证
- **错误提示**：输入框下方显示红色错误信息

---

## 五、前端项目结构

```
frontend/
├── h5-member/                    # H5 会员端
│   ├── src/
│   │   ├── api/                  # API 请求
│   │   │   ├── camp.ts
│   │   │   ├── payment.ts
│   │   │   └── progress.ts
│   │   ├── components/           # 通用组件
│   │   │   ├── CampCard.vue
│   │   │   ├── StatusTag.vue
│   │   │   ├── CheckinCalendar.vue
│   │   │   └── ProgressBar.vue
│   │   ├── views/                # 页面
│   │   │   ├── CampList.vue
│   │   │   ├── Enroll.vue
│   │   │   ├── QRCode.vue
│   │   │   └── Progress.vue
│   │   ├── router/
│   │   ├── stores/
│   │   └── utils/
│   └── package.json
│
├── admin-web/                    # 管理后台
│   ├── src/
│   │   ├── api/
│   │   │   ├── camp.ts
│   │   │   ├── member.ts
│   │   │   ├── refund.ts
│   │   │   ├── statistics.ts
│   │   │   └── system.ts
│   │   ├── components/
│   │   │   ├── DataCard.vue
│   │   │   ├── SearchForm.vue
│   │   │   ├── DataTable.vue
│   │   │   └── DetailDrawer.vue
│   │   ├── views/
│   │   │   ├── dashboard/
│   │   │   ├── camp/
│   │   │   ├── member/
│   │   │   ├── checkin/
│   │   │   ├── refund/
│   │   │   ├── statistics/
│   │   │   └── system/
│   │   ├── router/
│   │   ├── stores/
│   │   └── utils/
│   └── package.json
│
└── shared/                       # 共享代码（可选）
    ├── types/                    # TypeScript 类型定义
    └── utils/                    # 共享工具函数
```

---

## 六、路由设计

### 6.1 H5 会员端路由

```typescript
const routes = [
  { path: '/', redirect: '/camps' },
  {
    path: '/camps',
    name: 'CampList',
    component: () => import('@/views/CampList.vue')
  },
  {
    path: '/enroll/:campId',
    name: 'Enroll',
    component: () => import('@/views/Enroll.vue')
  },
  {
    path: '/qrcode/:paymentId',
    name: 'QRCode',
    component: () => import('@/views/QRCode.vue')
  },
  {
    path: '/progress/:memberId',
    name: 'Progress',
    component: () => import('@/views/Progress.vue')
  }
];
```

### 6.2 管理后台路由

```typescript
const routes = [
  { path: '/login', component: Login },
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      { path: 'dashboard', component: Dashboard },
      // 训练营管理
      { path: 'camps', component: CampList },
      { path: 'camps/create', component: CampCreate },
      { path: 'camps/:id', component: CampDetail },
      { path: 'camps/:id/edit', component: CampEdit },
      // 会员管理
      { path: 'members', component: MemberList },
      { path: 'members/:id', component: MemberDetail },
      // 打卡管理
      { path: 'checkins', component: CheckinList },
      { path: 'checkins/statistics', component: CheckinStatistics },
      // 退款审核
      { path: 'refunds', component: RefundList },
      { path: 'refunds/records', component: RefundRecords },
      // 统计报表
      { path: 'statistics', component: Statistics },
      // 系统管理 (仅超级管理员)
      { path: 'system/users', component: UserList, meta: { role: 'admin' } },
      { path: 'system/config', component: SystemConfig, meta: { role: 'admin' } },
      { path: 'system/logs', component: OperationLogs, meta: { role: 'admin' } }
    ]
  }
];
```

---

## 七、浏览器兼容性

### H5 会员端

- iOS Safari 13+
- Android Chrome 90+
- 微信内置浏览器
- 企业微信内置浏览器

### Web 管理后台

- Chrome 90+
- Edge 90+
- Firefox 88+
- Safari 14+

---

## 八、下一步

1. **项目初始化**：使用 Vite 创建 H5 和 Admin 项目
2. **组件开发**：实现通用组件
3. **页面开发**：按优先级实现各页面
4. **API 对接**：与后端接口联调
5. **测试优化**：功能测试和性能优化

---

**文档版本历史**：

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2025-10-24 | 初始版本 |
| v1.1 | 2025-10-24 | 与 PRD 和技术架构对齐，简化结构 |